// ============================================================
// Frontend configuration
// ============================================================
// Same-origin by default (server also serves the client/ folder).
// Change this if you host the frontend separately from the API.
const API_BASE_URL = window.location.origin + '/api';
